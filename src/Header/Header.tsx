import './Header.css';

const Header: React.FC = () => (
    <header className={'light-mode'}>
      <nav>
        <ul>
          <li><a href="/">Home</a></li>
        </ul>
      </nav>
    </header>
  );

export default Header;
